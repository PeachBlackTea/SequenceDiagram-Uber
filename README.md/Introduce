- Uber Sequence Diagram and Implementation

📌 프로젝트 개요
이 프로젝트는 Uber와 같은 차량 호출 서비스의 주요 프로세스를 모델링한 시퀀스 다이어그램과 이를 기반으로 한 JavaScript 구현을 포함합니다.  
일상 속 소프트웨어 사용 사례를 탐구하며, 시스템의 상호작용을 분석하고 간단한 프로토타입을 제작하는 데 목적이 있습니다.

---

🛠️ 프로젝트 구조
```plaintext
uber-sequence-diagram/
├── README.md                 # 프로젝트 개요 및 설명 파일
├── diagrams/
│   ├── sequence-diagram.mmd  # Mermaid 다이어그램 원본 및 이미지 파일
├── src/
     └── uber-app.js           # JavaScript 원시 코드
---

- 샘플 코드의 모듈 평가 결과 간략히 작성 (샘플 코드의 응집도, 결합도 평가)

샘플 코드는 높은 응집도와 낮은 결합도를 유지하며, 객체지향 설계 원칙을 준수합니다.
다만, 중앙화된 흐름 제어(RideApp 클래스의 역할 집중)은 장기적으로 관리가 복잡해질 수 있는 잠재적 단점으로 보입니다.
이를 개선하려면 이벤트 기반 설계나 인터페이스 분리를 통해 더 유연한 구조를 만들 수 있습니다.
